services:
  - type: web
    name: gold-nasdaq-telegram-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python gold_nasdaq_bot.py"
    healthCheckPath: /
